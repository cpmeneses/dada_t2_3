package dadat2;

public class InsertPatricia {
  
  //public void insert(String insertee, InterfaceNodePatricia head, int checked) {
    //hoja?
  //  if (head.isLeaf()) {
	  //nos va a dar lo mismo el caso del string vacio
  //    if (head.isRoot) {
  //      head.
//	  }
	  //se inserta lo que queda no mas con una hoja nueva
  //  } else {
	  //ver si algun edge de head tiene la misma letra inicial
//	}
//  }
  
}
